# Minimal requirements for backend deployment
# This excludes frontend dependencies and uses CPU-only PyTorch

# Core API
fastapi
uvicorn
python-dotenv
pydantic

# Data handling
numpy<2.0
pandas
pyarrow

# Search & embeddings (pre-computed, just need to load/search)
faiss-cpu
scikit-learn

# Text processing
regex
unidecode

# Sentence encoding (needed to encode user queries at runtime)
# PyTorch CPU-only (no CUDA = much smaller!)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0
transformers==4.44.0
sentence-transformers==2.2.2

# LLM integration (minimal)
requests
openai
